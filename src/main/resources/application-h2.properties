# ===============================
# SERVER CONFIG (Render compatible)
# ===============================
server.port=${PORT:8080}

# ===============================
# DATABASE (PostgreSQL - Render)
# ===============================
spring.datasource.url=${jdbc:postgresql://dpg-d7rgii6gkk3c738jlk40-a:5432/smartcity_gr3w}
spring.datasource.username=${smartcity_user}
spring.datasource.password=${KkISwBdLMOfUWSws5WLVqIlwoQaDCn5Y}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# JWT CONFIG (from Render env)
# ===============================
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# ===============================
# CORS
# ===============================
cors.allowed.origins=*

# ===============================
# LOGGING (optional)
# ===============================
logging.level.com.smartcity=DEBUG
logging.level.org.springframework.security=DEBUG